# homebrew-rift

A [Homebrew](https://brew.sh) tap for [Rift](https://github.com/Ankit-0202/rift), a small statically
typed language for configuration and automation.

```sh
brew install Ankit-0202/rift/rift
```

Or tap it first, then install:

```sh
brew tap Ankit-0202/rift
brew install rift
```
